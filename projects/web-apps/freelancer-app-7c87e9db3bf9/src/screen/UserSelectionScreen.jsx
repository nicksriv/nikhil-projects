import React from 'react';

import UserSelectionContainer from '@app/container/UserSelection/UserSelectionContainer';
const UserSelectionScreen = (props) => {
  return <UserSelectionContainer {...props}/>;
};

export default UserSelectionScreen;
