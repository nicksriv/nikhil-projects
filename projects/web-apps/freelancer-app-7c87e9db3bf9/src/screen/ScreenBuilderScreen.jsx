import ScreenBuilderContainer from "@app/container/ScreenBuilder";
import React from "react";

const ScreenBuilderScreen = (props) => {
  return (
    <>
      <ScreenBuilderContainer {...props} />
    </>
  );
};

export default ScreenBuilderScreen;
