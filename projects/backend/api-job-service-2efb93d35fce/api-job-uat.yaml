apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-job-service
  namespace: uat
spec:
  replicas: 1
  selector:
    matchLabels:
      app: api-job-service
  template:
    metadata:
      labels:
        app: api-job-service
    spec:
      spec:
      volumes:
        - name: my-vol
          hostPath:
            path: /storage
      containers:
        - name: api-job-service
          image: 392288302288.dkr.ecr.ap-south-1.amazonaws.com/job:latest
          imagePullPolicy: 'Always'
          volumeMounts:
            - name: my-vol
              mountPath: /var
          ports:
            - containerPort: 9090
          env:
            - name: PORT
              value: "9090"
      imagePullSecrets:
      - name: ecr-uat-auth        

---
apiVersion: v1
kind: Service
metadata:
  name: api-job-service
  namespace: uat
spec:
#  type: NodePort
  ports:
    - port: 9090
#      targetPort: 9090
#      nodePort: 30850
  selector:
    app: api-job-service
